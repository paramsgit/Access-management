{
  "name": "webapp",
  "private": true,
  "packageManager": "npm@11.3.0",
  "workspaces": [
    "./*",
    "editor/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "scripts": {
    "dev": "turbo run dev --filter=webapp",
    "build": "turbo run build"
  }
}